# Frontend API endpoints
VITE_BACKEND_URL=http://127.0.0.1:8001
VITE_AGENT_URL=http://127.0.0.1:8010

# Local-only QA helper. Vite disables this in production builds because the code is gated by import.meta.env.DEV.
VITE_ENABLE_DEV_GMAIL_SIGNIN=true